# Input and output

Required inputs are an absolute-path material CSV, Campaign YAML, BGM, category profile, and product/brand assets. Validate them before remote calls.

The run directory contains candidate analysis, music beats, visual/ad/JianYing plans, one continuous voiceover, clean and preview covers, MP4 outputs, validation reports, and `result.json`.

Use the root project README for commands and category extension instructions.

